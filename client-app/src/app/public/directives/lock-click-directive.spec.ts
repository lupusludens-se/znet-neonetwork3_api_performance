import { LockClickDirective } from "./lock-click-directive";


describe('LockClickDirective', () => {
  it('should create an instance', () => {
    const directive = new LockClickDirective();
    expect(directive).toBeTruthy();
  });
});
